import cv2
#we are import the module cv2
#read image from loacl drive or any other place we give the path only.
#my path is 'F:\images'
image = cv2.imread("F:/images/naga1.jpg")
#now show the output
cv2.imshow("output",image)
cv2.waitKey(0)
